apiVersion: v1
kind: Pod
metadata:
  namespace: test
  name: httpd-test1
  labels:
    env: test
    app: httpd
spec:
  containers:
    - image: httpd
      name: httpd-container
      ports:
        - containerPort: 80